Hi, thanks for the review comment. I have two approaches in mind to address this — wanted to check which one you'd prefer before making the changes:

Option 1 — Extract to a separate shell script
Move all the bash logic from the Publish Terraform modules to JFrog registry step into .github/scripts/publish_modules.sh with proper section comments. The pipeline step would then just call:
bash .github/scripts/publish_modules.sh
Pros: independently testable locally, can be linted with shellcheck, cleaner YAML.

Option 2 — Keep inline in the pipeline with comments
Keep the bash inside the run: block as-is but add # section comments to explain each part (constants, publish_module() function purpose, main loop logic).
Pros: everything stays in one file, easier to follow without switching files.

Could you let me know which approach you'd like me to go with? Happy to proceed with either.
